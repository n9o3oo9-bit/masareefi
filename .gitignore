.DS_Store
*.log
.claude/

# نسخ البيانات المصدَّرة — لا ترفعها، فيها مصاريفك
مصاريفي-*.json

# ملفات خط ثمانية — تُستخدم محليًا للبناء فقط.
# ترخيص الخط يمنع إعادة توزيع الملفات، لذا لا تُرفع لمستودع عام.
# الخط مضمَّن داخل index.html كاستخدام مرخَّص داخل التطبيق.
# لإعادة البناء على جهاز آخر: نزّل الخط من font.thmanyah.com إلى fonts/
fonts/
